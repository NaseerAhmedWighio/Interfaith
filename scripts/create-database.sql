-- Create the database for Interfaith Peace Bridge
-- Run this first before running migrations

-- Connect to PostgreSQL as superuser and run:
-- CREATE DATABASE interfaith_db;

-- Then connect to interfaith_db and enable UUID extension:
CREATE EXTENSION IF NOT EXISTS "pgcrypto";
